base-url: /api/v1

spring:
  application:
    name: voting

  jpa:
    properties:
      hibernate:
        enable_lazy_load_no_trans: true
    show-sql: true

  mvc:
    hiddenmethod:
      filter:
        enabled: true

logging:
  level:
    root: INFO